#include "emulator.h"
#include "disasm.h"

void printState(EmulatorState *state)
{
    
}

int main(int argc, char *argv[])
{
    uint8_t memory[] = {
        0x00,               // NOP

    };
    return 0;
}
